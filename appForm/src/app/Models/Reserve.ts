import { Time } from "@angular/common";

export interface Reserve {
  id: string;
  timeId: string;
  tableId: string;
  smoking: string;
  name: string;
  email: string;
  phone: string;
  note: string;
}
